@model IEnumerable<KnowledgeTestMedAjax.Models.AddressBook>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tnr)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastUpdate)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tnr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastUpdate)
            </td>
            <td>
                @*@Ajax.ActionLink("Edit", "Edit", new { id = item.AddressBookId },
                new AjaxOptions
                {
                    HttpMethod = "GET",
                    UpdateTargetId = "displayItem",
                    InsertionMode = InsertionMode.Replace
                }) |
                @Ajax.ActionLink("Details", "Details", new { id = item.AddressBookId },
                new AjaxOptions
                {
                    HttpMethod = "GET",
                    UpdateTargetId = "displayItem",
                    InsertionMode = InsertionMode.Replace
                }) |
                @Ajax.ActionLink("Delete", "Delete", new { id = item.AddressBookId },
                new AjaxOptions
                {
                    HttpMethod = "GET",
                    UpdateTargetId = "displayList",
                    InsertionMode = InsertionMode.Replace,
                    Confirm = "Are you sure to delete this item?"
                })*@

                @Html.ActionLink("Edit", "Edit", new { id = item.AddressBookId }) |
                @Html.ActionLink("Details", "Details", new { id = item.AddressBookId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.AddressBookId })
            </td>
        </tr>
    }
</table>